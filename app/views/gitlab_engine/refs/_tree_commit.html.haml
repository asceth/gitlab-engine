- if tm
  %strong= link_to "[#{tm.user_name}]", gitlab_engine.project_team_member_path(@project, tm)
= link_to truncate(content_commit.safe_message, :length => tm ? 30 : 50), gitlab_engine.project_commit_path(@project, content_commit.id), :class => "tree-commit-link"
