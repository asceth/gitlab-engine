= render "gitlab_engine/repositories/branches_head"

%table.admin-table
  %thead
    %tr
      %th Name
      %th Last commit
      %th Updated at
      %th
  - @activities.each do |update|
    = render "gitlab_engine/repositories/branch", :branch => update.head

