%ul.nav.nav-tabs
  %li{ :class => "#{'active' if current_page?(gitlab_engine.project_path(@project)) }" }
    = link_to gitlab_engine.project_path(@project), :class => "activities-tab tab" do
      %i.icon-home
      Show
  %li{ :class => " #{'active' if (controller.controller_name == "team_members") || current_page?(gitlab_engine.project_team_members_path(@project)) }" }
    = link_to gitlab_engine.project_team_members_path(@project), :class => "team-tab tab" do
      %i.icon-user
      Team
  %li{ :class => "#{'active' if current_page?(gitlab_engine.files_project_path(@project)) }" }
    = link_to gitlab_engine.files_project_path(@project), :class => "files-tab tab " do
      Attachments
  %li{ :class => " #{'active' if (controller.controller_name == "snippets") }" }
    = link_to gitlab_engine.project_snippets_path(@project), :class => "snippets-tab tab" do
      Snippets

  - if can? current_user, :admin_project, @project
    %li.right{:class => "#{'active' if controller.controller_name == "deploy_keys"}"}
      = link_to gitlab_engine.project_deploy_keys_path(@project) do
        %span
        Deploy Keys
    %li.right{:class => "#{'active' if controller.controller_name == "hooks" }"}
      = link_to gitlab_engine.project_hooks_path(@project) do
        %span
        Hooks
    %li.right{ :class => "#{'active' if current_page?(gitlab_engine.edit_project_path(@project)) }" }
      = link_to gitlab_engine.edit_project_path(@project), :class => "stat-tab tab " do
        %i.icon-edit
        Edit
