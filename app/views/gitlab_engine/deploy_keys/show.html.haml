= render "gitlab_engine/repositories/head"
%h3= @key.title
%hr
%pre= @key.key
.actions
  = link_to 'Remove', gitlab_engine.project_deploy_key_path(@key.project, @key), {:data => {:confirm => 'Are you sure?'}, :method => :delete, :class => "danger btn delete-key"}
  .clear
