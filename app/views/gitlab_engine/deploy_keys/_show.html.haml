%tr
  %td
    %a{:href => gitlab_engine.project_deploy_key_path(key.project, key)}
      %strong= key.title
  %td
    %span.update-author
      Added
      = time_ago_in_words(key.created_at)
      ago
  %td
    = link_to 'Remove', gitlab_engine.project_deploy_key_path(key.project, key), {:data => {:confirm => 'Are you sure?'}, :method => :delete, :class => "danger btn delete-key small right"}

