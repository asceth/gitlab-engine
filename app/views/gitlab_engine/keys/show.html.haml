%h3
  Public key:
  = @key.title
  %small
    created at
    = @key.created_at.stamp("Aug 21, 2011")
.back_link
  = link_to gitlab_engine.keys_path do
    &larr; To keys list
%hr

%pre= @key.key
.actions
  = link_to 'Remove', gitlab_engine.key_path(@key), {:data => {:confirm => 'Are you sure?'}, :method => :delete, :class => "btn danger delete-key"}
