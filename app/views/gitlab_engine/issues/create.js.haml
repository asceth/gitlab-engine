- if @issue.valid?
  :plain
    switchFromNewIssue();
    $("#issues-table").prepend("#{escape_javascript(render(:partial => 'gitlab_engine/issues/show', :locals => {:issue => @issue}))}");
    $.ajax({type: "GET", url: location.href, dataType: "script"});
- else
  :plain
    $("#new_issue_dialog").empty();
    $("#new_issue_dialog").append("#{escape_javascript(render('gitlab_engine/issues/form'))}");
    $('select#issue_assignee_id').chosen();
